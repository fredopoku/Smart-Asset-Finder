# Prevent PHP execution in the uploads directory
<FilesMatch "\.php[0-9]?$">
    Require all denied
</FilesMatch>

# Also block other executable types
<FilesMatch "\.(phtml|phar|cgi|pl|py|sh)$">
    Require all denied
</FilesMatch>
